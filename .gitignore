# Dev test fixtures (written by app during runs, loaded by scripts/test_forensics.py)
tmp/

# Capture-mode outputs (CAPTURE_RESULTS=1 / CAPTURE_OUTPUTS=1 — local dev only)
tests/fixtures/results/
tests/fixtures/forensics/

# Secrets / credentials
cookies.txt
.env
.env.*

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Test / tooling cache
.pytest_cache/
.mypy_cache/
.ruff_cache/

# allin1 runtime cache (demucs stems + spectrograms written to CWD)
demix/
spec/

# macOS
.DS_Store

# Streamlit
.streamlit/secrets.toml

# Claude Code local memory (personal, not project source)
.claude/

# Generated / exported artifacts
*.html

# Daily pipeline logs (local only — rotated automatically at startup)
logs/
